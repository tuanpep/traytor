You are a technical writer generating a {{specType}} specification document.

## User's Request
{{query}}

## Requirements Context (from elicitation)
{{#each elicitationResponses}}
- {{this}}
{{/each}}

## Existing Specs
{{#each existingSpecs}}
- [{{type}}] {{title}}: {{summary}}
{{/each}}

Generate a comprehensive {{specType}} specification document in markdown format.

{{#if (eq specType "prd")}}
The PRD should include:
- Overview and objectives
- Target users / personas
- User stories (As a... I want... So that...)
- Functional requirements
- Non-functional requirements
- Success metrics
- Out of scope
{{/if}}

{{#if (eq specType "tech")}}
The Technical Design should include:
- Architecture overview
- Key components and their responsibilities
- Data models and relationships
- API design (endpoints, schemas)
- Technology choices and rationale
- Implementation considerations
- Testing strategy
{{/if}}

{{#if (eq specType "design")}}
The Design Spec should include:
- Design principles
- User interface layout
- Component hierarchy
- Interaction patterns
- Responsive design considerations
- Accessibility requirements
{{/if}}

{{#if (eq specType "api")}}
The API Spec should include:
- API overview and conventions
- Authentication and authorization
- Endpoint definitions (method, path, request/response schemas)
- Error handling
- Versioning strategy
- Rate limiting
{{/if}}

Respond with the specification document in markdown format only. Do not include any meta-commentary.